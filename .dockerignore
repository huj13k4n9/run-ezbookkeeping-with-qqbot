# 构建上下文瘦身。注意 agent / .agents 是**故意排除**的：
# 它们由 compose 从宿主机挂载，不烤进镜像。
.git
.gitignore
.venv
venv
__pycache__/
*.pyc
*.pyo
.pytest_cache

# 运行时数据：绝不进镜像
data/
.env
.env.*
!.env.example

# 文档与临时文件
*.md
tmp_*
.DS_Store
